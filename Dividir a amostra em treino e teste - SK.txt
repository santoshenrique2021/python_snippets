#Dividir a amostra em treino e teste

##Ponto de partida: Através do data frame original, dividi-se os dados em duas array:
##x - conjunto de variáveis explicativas
##y - variável resposta


from sklearn.model_selection import train_test_split

treino_x, teste_x, treino_y, teste_y = train_test_split(x, y, random_state = 1935, test_size = 0.25, stratify = y)

#Finalidade: Este procedimento tem como finalidade definir as bases de treino e teste antes de executar o EDA e a modelagem